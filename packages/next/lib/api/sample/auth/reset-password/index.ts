/* eslint-disable */
export type Methods = {
  /** Reset user password with a code (resetToken) */
  post: {
    status: 200

    /** response */
    resBody: {
      foo: string
    }

    reqBody: {
      foo: string
    }
  }
}
